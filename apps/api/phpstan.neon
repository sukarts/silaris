includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: 5
    paths:
        - src
        - app
    reportUnmatchedIgnoredErrors: false
    # Baseline : bruit propriétés magiques Eloquent — dette suivie, à réduire
