{
  "name": "silaris",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "api:openapi": "cd apps/api && php artisan scramble:export --path=../../packages/api-client/openapi.json",
    "api:client": "pnpm --filter @silaris/api-client generate"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=22"
  }
}